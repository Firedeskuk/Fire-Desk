# Night 1 progress log

Kept by Claude Code during the unattended run of docs/NIGHT-1-TASK.md.
Before each step: what is about to happen. After each step: what happened.
If context is compacted: re-read this file first, continue from the last unfinished step.

## Step 0: setup

- About to: create branch feat/mvp-night-1 from up to date main, create .env.local from .env.example with placeholders, start this file.
- Done: branch created from origin/main (d07a114). .env.local created with placeholder values, it is git ignored and not committed.
- Decision: docs/mockups does not exist in the repo (SPEC.md and the task reference it). Layouts are taken from the task text and the tokens in CLAUDE.md section 9.
- Decision: the Claude Code session was started on branch claude/night-1-task-yy281e. The task says feat/mvp-night-1. Work happens on feat/mvp-night-1, the same commits are also pushed to claude/night-1-task-yy281e so the session record stays complete. Pull request is opened from feat/mvp-night-1.

## Step 1: align repo with the database

- About to: delete 0001_init.sql and 0002_sync.sql, check header lines, check docs references, fix stale comment lines inside the timestamped files that still say 0001 or 0002.
- Done: deleted 0001_init.sql and 0002_sync.sql. Header lines were already correct. CLAUDE.md, SPEC.md, SYNC-PROTOCOL.md and SETUP.md section B were already correct. Fixed three stale comment lines inside the timestamped files (Requires, RLS gap from, End of) so no 0001 or 0002 reference remains. Comment only, no SQL statement changed.

## Step 2: scaffold

- About to: scaffold Next.js into a temp dir and move files in, install allowed packages, theme.css, root layout with theme toggle, manifest, PNG icons via a node script, service worker with @serwist/next (fallback: hand written public/sw.js).
- Done: Next.js 16.3.5 scaffolded in a temp dir and moved in (TypeScript, App Router, ESLint, no Tailwind, no src, alias @/*). Installed: @supabase/supabase-js 2.116.0, dexie 4.4.6, dexie-react-hooks 4.4.0, @serwist/next 9.5.12, serwist 9.5.12, browser-image-compression 2.0.2, dev: vitest 4.1.11, fake-indexeddb 6.2.5, @vitejs/plugin-react 5.2.0, jsdom 30.0.1. styles/theme.css with all tokens, root layout with theme boot script and ThemeToggle, manifest, PNG icons from scripts/make-icons.mjs, service worker with @serwist/next (built on the first attempt, build script is "next build --webpack" because serwist is a webpack plugin). Offline fallback page at /offline. Vitest config and setup with fake-indexeddb. README.md in every folder that stays empty tonight.
- Decision: plain npm install failed with an npm arborist error (Cannot read properties of null, reading edgesOut) caused by peer dependency resolution. Installed with --legacy-peer-deps and committed .npmrc with legacy-peer-deps=true so npm install works the same for everyone.
- Decision: @testing-library/react was not installed. It needs @testing-library/dom at runtime, which is not on the allowed list, so it cannot work. Tests use vitest plus fake-indexeddb only.
- Decision: git push from this session is refused with HTTP 403 (the Claude GitHub App is not installed for the Firedeskuk organisation). The GitHub API through the MCP connector works, so every step is committed locally and then mirrored to the remote branch with the API, one commit per step. The API accepts text only, so the two PNG icons are in the local commits but cannot be uploaded this way. They are regenerated with npm run icons.
- Decision: build passes with lint rule react-hooks/set-state-in-effect active (new in eslint-config-next 16). Components read browser state with useSyncExternalStore or set state inside async callbacks, never synchronously in an effect body.

## Step 3: Supabase client and auth

- About to: lib/supabase/client.ts, lib/supabase/types.ts (hand written from the SQL), lib/local/session.ts, login page, route guard, logout with outbox check.
- Blocker (recorded during step 3): the GitHub API route is also refused. Creating the branch through the API fails with "403 Resource not accessible by integration", so the Claude GitHub integration has no write access to Firedeskuk/Fire-Desk at all tonight. Decision: keep committing locally on feat/mvp-night-1 after every step, retry git push once after every step (cheap), and at the end deliver a git bundle of the branch as a file in the session so nothing is lost if the push never works. Tomasz or Piotr can then push it with one command (written in the report).
- Done: lib/supabase/client.ts (lazy singleton, localStorage session under key firedesk-auth, no cookies), lib/supabase/types.ts (every table, view, enum and both RPC shapes, written from the SQL; rows are type aliases because supabase-js needs index signature compatible rows), lib/supabase/auth.ts (signIn loads the profile and stores it locally, signOut refuses with "N changes are not yet sent. Sync first." when pendingCount is above zero, refreshProfile), lib/local/session.ts (local profile, device id created once, roleHome, newId), login page at /login (one column, Fire Desk logo text, 48 px inputs, Sign in button, error under the button, offline and unconfigured messages), components/shared/RouteGuard.tsx (no session goes to /login with ?next=, wrong role goes to its role home), root page redirects by role. The visible Logout button lands in step 4 together with the outbox count it depends on, and is placed in the office header (step 8) and field settings (step 6).
- Decision: RouteGuard trusts the local profile only. It does not call Supabase, so a downloaded building opens offline even after the access token expired. Sync surfaces an auth problem separately (step 5).

## Step 4: lib/local (Dexie)

- About to: lib/local/db.ts, index.ts (get, list, put, patch, transaction), outbox.ts, blobs.ts, LogoutButton, vitest tests with fake-indexeddb.
- Done: lib/local/db.ts (Dexie "firedesk", 13 mirror tables with origin, outbox with ++seq and [status+seq], blobs by path, sync_state, session; indexes on building_id, asset_id, finding_id, remedial_item_id, floor_id, qr_code, inspection_id, template_id), lib/local/index.ts (get, list, put, patch, putPhoto, transaction, replaceServerRows, markSynced, sync_state and session helpers, removeBuilding, clearAll, storageUsage), lib/local/outbox.ts (enqueueUpsert, enqueuePatch, enqueuePhoto, pending, ready with depends_on and backoff, markSending, markDone, markFailed with 5 s, 30 s, 2 min, 10 min, hourly and failed after 10, releaseSending, recoverStaleSending, counts, listFailed, retryFailed, purgeDone), lib/local/blobs.ts (putBlob, getBlob, release, blobUsage, blobUrl), components/shared/LogoutButton.tsx. Tests in tests/local.test.ts: 9 passing (write creates both rows, failed transaction creates neither, patch carries only changed fields, seq order, photo dependencies, backoff and failed after 10, replaceServerRows keeps local rows, soft delete filter).
- Decision: the photo upload row does not depend on the finding row, only the finding_photos row depends on the upload (and on the finding row when the screen passes it). Storage and table writes are independent on the server, so the upload can start straight away.

## Step 5: lib/sync

- About to: lib/sync/status.ts, download.ts, push.ts, triggers.ts, lib/photos/compress.ts, tests with a mocked Supabase client.
- Done: lib/sync/status.ts (store with online, pendingCount, failedCount, syncing, progress, lastError, lastSyncAt, authProblem, useSyncStatus hook, describeSyncStatus for the pill), lib/sync/push.ts (Web Lock firedesk-sync with in memory fallback, session refresh first, ready rows in seq order, batches of 50 into sync_push, photos one at a time to bucket photos with upsert true, ok and ignored_older mark done and flip origin, error marks failed with backoff, network errors leave rows pending, auth errors pause sync and set authProblem, blob released when the photo row is done, uploaded_at filled at send time), lib/sync/download.ts (download_building into mirror tables in one transaction, local rows untouched, floor plans via signed URLs into blobs, sync_state written, describeAge), lib/sync/triggers.ts (online, visibilitychange, 60 s timer while pending, syncNow, stale sending rows recovered at start), lib/photos/compress.ts (browser-image-compression, 1600 px, 0.3 MB, jpeg, width and height), components/field/SyncBoot.tsx. Tests in tests/sync.test.ts: 8 passing (order and done, error row does not block, dropped connection leaves rows pending, batches of 50, photo upload then row then blob released, offline does nothing, auth failure pauses, download keeps local rows and caches the floor plan). vitest config renamed to vitest.config.mts to silence a Vite ESM warning.
- Decision: the worker never imports supabase-js directly. triggers.ts injects the real client once, tests inject a fake. This keeps the field bundle free of anything the worker does not need and makes the worker testable.

## Step 6, 7, 8: field and office screens

- About to: shared pieces first (field layout with SyncBoot and guards, FieldHeader, SyncPill with failed list and retry, FieldSettings, office layout and header, lib/sync/index.ts with building list refresh and download using the real client, lib/pricing, lib/due.ts). Then inspector screens, remedial screens and office screens in parallel by three agents with disjoint folders, then a review pass, then the three commits in order.
- Done (step 6, inspector): /buildings list with online refresh, download state and Download or Refresh button, /buildings/[id] floors with asset and due counts plus a "No floor" group, /buildings/[id]/floors/[floorId] assets, /buildings/[id]/assets/[assetId] door screen (title, subtype, last inspected, cycle, Scan QR camera stub, checklist from the active template with yes and no pills, number, text and select inputs, one finding per failing question created inside one transaction, cancelled again when the answer changes back, "N findings created", Photo and Next door, automatic completion with overall_result and the provisional next due date, read only view of the last completed inspection with "New inspection"), /buildings/[id]/findings/[findingId] (description, severity pills, photo capture with compression into blobs and outbox, floor plan pin stored as 0 to 1). Components under components/field/inspector.
- Done (step 7, remedial): /works list, /works/[buildingId] with "N items, M done, K left", rows with description, detail and status pill, /works/[buildingId]/items/[itemId] with Start, Photo after and Mark done (disabled until a photo after exists), photos into remedial_photos. No finding or inspection appears anywhere in the remedial screens. Components under components/field/remedial.
- Done (step 8, office): /dashboard with four metric cards and the Alerts card (one row per building, red, yellow, orange, green from lib/due), /clients, /office/buildings with the building editor, floors editor with floor plan upload to bucket floorplans, assets table with add and edit, /projects, /settings/pricing, /settings/templates with add and reorder, /settings/users with role dropdown and building assignments for remedial users, /quotes, /quotes/new?building= and /quotes/[id]. Components under components/office.
- Done (step 9): supabase/seed.sql, invented names only, fixed uuids, on conflict do nothing, not run.
- Review: six review agents (spec and rules lens per area) produced 28 findings, three fix agents applied 21 of them (duplicate inspection on a fast double tap, No Yes No stacking findings, ids read from useParams, remedial photo paths from the URL instead of the item row, floor plan replace on the same storage path, PDF plans the phone cannot render, quote total computed in JS floats instead of read back from the database, quote save leaving an orphan quote, first price item silently chosen, removed doors counted as due, 40 px office buttons). The rest were requests for shared files and are handled below or listed in the report.
- Decision: the office building register lives at /office/buildings because /buildings is the inspector list (two pages cannot share one path across route groups). The header link says "Buildings" as before.
- Decision (offline shells): Next renders every field route with a placeholder id "_" as static HTML (generateStaticParams in small server layouts), the service worker precaches those six shells plus the top level pages, and serves the matching shell when a never visited door, floor, finding or work item is opened offline. Pages read their ids from the browser URL through lib/useFieldParams. Verified in headless Chromium with scripts/offline-check.mjs: 14 of 14 checks pass (door, floor, finding and work item screens open offline without a prior visit, an answer offline queues inspection, answer, finding and completion rows, the pill shows "Offline, 4 to sync", Start on a work item queues a patch).
- Bug found by that check and fixed: outbox change listeners fired inside the Dexie transaction zone ("Transaction committed too early"). They now fire after commit, outside the zone.
- Decision: the sync pill refreshes its counts after every local write (outbox listeners), and a write while online starts a sync run 1.5 s later. .btn-small and .pill-button are 48 px tall, as CLAUDE.md section 9 asks.
